
<div class="search_form">
  <div class="search_form">
    <% if admin_signed_in? %>
      <%= form_with url: admin_customer_search_path, local: true, method: :get do |f| %>
        <div class="row">
          <div class="col-md-10">
            <%= f.text_field :word ,placeholder:"会員を検索する",class: "form-control" %>
          </div>
          <div class="col-md-2">
            <%= f.submit "検索", class: "btn btn-sm btn-outline-dark" %>
          </div>
        </div>
      <% end %>
    <% end %>
  </div>
</div>
