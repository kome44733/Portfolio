
<div class="search_form">
  <%= form_with url: search_path, local: true, method: :get do |f| %>
    <%= f.text_field :word %>
    <%= f.hidden_field :profession,value: params[:id] %>
    <%= f.submit "検索", class: "btn btn-sm btn-outline-light" %>
  <% end %>
</div>

